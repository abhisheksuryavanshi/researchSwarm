"""Unit tests for search logic (T024).

Search logic is now simple SQL filtering — most meaningful tests are in
the contract tests that hit the actual endpoint. This file validates
the search_tools function signature and basic behavior.
"""
