# API base (no trailing slash).
# Dev: leave unset to use the Vite proxy (same origin as the UI — no CORS issues).
# Or set http://127.0.0.1:8000 — then CORS_ORIGINS on the backend must include your UI origin.
# Production build: set to your public API URL.
# VITE_API_BASE_URL=http://localhost:8000
